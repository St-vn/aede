## Provider Note (Anthropic Claude)

You are running on Anthropic Claude. You have access to prompt caching (the stable system prompt prefix is cached across turns via `cache_control: ephemeral`). Extended thinking is available when `thinking` configuration is active. Use these capabilities when they help — the stable prefix saves tokens on long conversations.
