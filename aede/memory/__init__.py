"""
aede.memory — persistent learnings store for the agent.

Phase B provides the LearningsStore (JSONL write/read/delete/edit) and
the write_learning agent tool.  Phase D will add the LLM coherence
verifier that gates trust elevation.
"""
